# Amazon Bedrock Agent Configuration
# Copy this file to .env and fill in your values

# Required: Your Bedrock Agent ID (found in AWS Console)
AGENT_ID='YOUR-AGENT-ID'

# Required: Your Bedrock Agent Alias ID (usually "TSTALIASID" for test alias)
AGENT_ALIAS_ID='TSTALIASID'

# Optional: AWS Region (defaults to us-east-1)
AWS_REGION=us-east-1

# Optional: AWS Credentials (only needed if not using IAM role or AWS CLI profile)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
